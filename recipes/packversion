add ../src/* in src/
add ../install.sh

root_dir ..
> mkdir -p releases
target_dir releases
pack rt.tar.gz

add ./src/* in src/
add ./install.sh
add ./recipes/install as install

target_dir ./releases
pack rt.rt